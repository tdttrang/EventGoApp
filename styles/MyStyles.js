import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        flex:1,
        marginTop: 60,
        justifyContent: "center",
        alignItems: "center"
    }, event: {
        fontSize: 30,
        fontWeight: "bold",
        color: "blue"
    }
}) 